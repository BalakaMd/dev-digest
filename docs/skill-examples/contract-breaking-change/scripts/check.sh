#!/bin/sh
# Deliberately present to demonstrate the import boundary: DevDigest lists this
# file as "ignored" and never extracts, stores, or runs it. A skill is text only.
echo "this script is never executed by DevDigest"
